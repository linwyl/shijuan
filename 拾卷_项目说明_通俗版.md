# 拾卷项目说明（通俗版）

整理日期：2026年9月10日  
对应程序：本地 HTML 0.4.1 版；详细项目说明 V0.7.1  
这份说明根据当前程序、已有说明和项目修改记录整理，方便不懂编程的人了解项目。

## 一、这个项目是做什么的？

**拾卷可以把一份带有题目和参考答案的 Word 或 PDF 试卷，变成可以点击选项的练习卡片。**

使用过程就是：

**打开网页文件 → 导入试卷 → 核对识别结果 → 逐题选择 → 立即看本题答案。**

例如，原试卷有一道题，选项是 A、B、C、D、E，参考答案是 E。程序会把这道题做成一张卡片。你选择一个选项后，卡片显示参考答案 E，并提示本次选择是否正确。

这里显示的答案来自原试卷。原文没有答案，或者题目和答案对不上时，程序会提示核对，不会自行编出答案。参考答案本身如果有错误，也需要你修正。

这是一个面向个人复习的本地工具。项目目前由你一个人负责，每周投入约10—15小时，优先把“导入、核对、练习”这条使用流程做好。

## 二、目前已经有什么功能？

| 功能 | 用普通话解释 |
| --- | --- |
| 导入 Word | 可以读取 `.docx`，也支持旧版 `.doc`。旧版文件里的特殊公式和图形，保留效果有限。 |
| 导入 PDF | 支持能选中文字的 PDF、扫描成图片的 PDF，以及两种页面混在一起的 PDF。 |
| 读取扫描文字 | 内置“图片里的文字识别”功能，也叫 OCR。扫描页的结果必须先核对。 |
| 多种语言 | 支持中文、英文和中英文混合；扫描识别内置简体中文和英文模型。 |
| 单选题卡片 | 明确支持 A—E 五个选项，包括答案为 E 的题目；也兼容使用 A—H 标签的2—8个选项。原题只有四项，就保持四项。 |
| 按原文顺序出卡片 | 原文先出现哪道题，就先显示哪道题。即使原题号是1、3、2，也不会擅自重新排序。 |
| 检查题目和答案的对应 | 优先按所属大题和题号查找答案。缺少一题答案时，不把后面的答案往前挪；对应关系不清楚时提示核对。 |
| 点击后显示答案 | 未作答时显示题干、必要的阅读材料和选项。选择后立即显示本题参考答案，不需要再点“提交”。 |
| 修改识别结果 | 可以逐题修改题干、选项、答案及公式，核对确认后再练习。 |
| 数学内容显示 | Word 原生公式可以重新排版；PDF 公式优先保留对应题干或选项的原图。 |
| 重新练习 | 清空当前试卷的选择记录，从头再做。首次选择后不能直接改选，需要重新练习。 |
| 自动记忆 | 自动保存试卷、公式图片、原文件副本、已确认的修改和最新作答进度，重开后恢复最近试卷。 |
| 已保存试卷 | 可以保留多份试卷，点击列表继续练习；即使同名，重新导入也会建立独立记录。 |
| 手动删除 | 点击列表的“删除”或“删除本卷记录”，确认后移除这份保存记录，不影响其他记录和电脑里的原试卷。 |
| 本地使用 | 推荐通过本地启动器打开，识别和保存都在自己的电脑上进行，不需要登录或连接外部服务。 |

页面没有示例题。没有保存记录时等待导入，有记录时恢复最近打开的试卷。部分题目需要核对时，练习区的卡片数可能少于识别到的题目数；修正并确认后，它们会回到原来的位置。

## 三、第一次怎么使用？

1. **准备试卷。** 请保证题目和答案在同一份文档中。题号、选项标签和答案题号越清楚，越容易识别。
2. **打开程序。** 推荐双击 `启动拾卷.cmd`，用同一个浏览器的普通窗口打开固定地址 `http://127.0.0.1:17839/`。启动器会尝试使用电脑现有的 Node.js 或 Codex 自带运行环境；其他电脑没有这些环境时需先安装 Node.js 22.13或更新版本。
3. **导入文件。** 选择或拖入一份 Word 或 PDF。扫描文件会慢一些，页面会显示处理进度，也可以取消。
4. **检查“识别核对”。** 对照原文检查题干、全部选项和参考答案。有问题时点击“核对 / 修改此题”，修正、勾选确认并保存。使用过扫描识别的 PDF，整份试卷都需要逐题核对。
5. **进入“练习卡片”。** 每题选择一个选项，立即查看本题答案和正误。其他未作答题目的答案不会因此一起展开。
6. **按需继续、重做或删除。** 点击“已保存试卷”继续上次练习。“重新练习”保留试卷、清空该卷作答，并记住清空后的状态；“删除”确认后移除对应保存记录。

**左侧显示“已自动保存”后，刷新或关闭再打开会恢复已保存内容。** 修改题目时，要先点击编辑窗口里的保存按钮；尚未确认的草稿不会保存。保存失败会提示并提供重试，不会把“尚未保存”当成成功。

数据保存在当前浏览器的本地数据库里，不是 MySQL，也不在网页文件或源码压缩包里。请固定浏览器和打开地址；更换电脑、使用隐私窗口或清理浏览器数据可能导致记录不可用。当前还没有跨设备同步或记录导出备份，请保留原试卷。升级前已丢失的临时内容无法找回。

### 公式显示不完整时怎么处理？

Word 和 PDF 的处理方法不同：

- **Word 原生公式：** 指在 Word 里使用“插入公式”等功能编写的公式。程序会尽量读出公式结构，再显示成分式、根号、上下标、积分、求和、矩阵等正常样子。无法完整转换的内容仍需核对。旧版 `.doc` 和某些插入式公式对象，不保证能转成可编辑公式。
- **PDF 公式：** 优先把原题中这一小块内容完整剪下来，放到对应卡片里。这样可以保留分数线、根号和符号的位置。这表示“保留原来的样子”，不代表程序已经理解公式或能把所有公式转成可编辑文字。

PDF 自动保留的内容不完整时，可以自己框选：

1. 打开该题的“核对 / 修改此题”。
2. 展开“从 PDF 原页框选题干或选项（保留公式）”。
3. 选择页码，再选择要替换的是“题干”还是某个选项，例如“选项 E”。
4. 框住**完整题干或一个完整选项**，把其中的文字、公式和必要图表一起选进去。不要只框一个公式，否则可能漏掉这部分的其他文字。
5. 确认选区没有参考答案、解析或其他题目，再点“添加选区原图”。跨页内容可以按阅读顺序分几次添加。
6. 检查预览，确认完整后保存。选错了可以移除片段，或改回文字、公式显示。

核对界面里的完整原页可能包含答案；练习卡片不会自动使用可能带答案的整页图片。

## 四、以前遇到过什么，怎么处理的？

下面分成三类：用户实际反馈的故障、开发检查发现的问题，以及后来提出的新需求。新增功能和修改文字不算程序报错。

### 1. 实际故障：导入扫描 PDF 时出现 Buffer 报错

**当时的现象：** 页面提示 `ReferenceError: Buffer is not defined`，扫描识别无法继续。

**原因：** 图片文字识别组件在解压识别资料时，用到了浏览器里没有的一种工具。早期测试环境恰好提供了这个工具，所以没有及时发现浏览器中的问题。

**怎么解决：** 改用浏览器自带的解压功能，先把内置的中英文识别资料解压好，再交给识别组件。用户不需要自己安装或补充 `Buffer`。

**检查结果：** 已在不提供上述工具、并禁止联网的独立测试环境中重现旧错误，再检查修复后的扫描导入；相关自动检查已通过。完整的浏览器实际操作验收仍需另做。

### 2. 实际问题：Word 数学公式显示不完整

**当时的现象：** 公式可能只剩一段提示，或者分式、上下标等结构没有完整显示出来。

**原因：** Word 公式不只是普通文字。如果只提取文字，就容易丢掉数学结构。

**怎么解决：** 增加专门的 Word 原生公式转换和排版功能，处理分式、根号、上下标、积分、求和、矩阵、方程组等，并增加公式编辑预览。

**现在的情况：** 已支持常见原生公式结构；特殊或无法识别的结构仍提示核对。旧版 Word、嵌入式 MathType 等公式对象，不能保证完整转换。

### 3. 实际问题：PDF 公式读成文字后丢失排版

**当时的现象：** 分数上下两行、根号、指数等内容，提取后可能混在一起，或者漏掉符号。

**原因：** PDF 主要记录页面上“什么内容放在什么位置”，不一定保存完整的数学结构。扫描识别也可能把公式当普通文字读。

**怎么解决：** 根据页面位置，分别保留完整题干和各个选项的原图；自动判断不清楚时，提供原页让用户手工框选。遇到复杂分栏或边界不明确的文件，会停止自动替换图片，先让用户核对。

**现在的情况：** 通过保留原图改善公式完整呈现，并提供人工补齐的方法。模糊原件、错误选区和复杂排版仍需要人工处理，不能称为“所有公式都已准确识别”。

### 4. 开发检查：阅读材料可能提前露出答案

**发现的问题：** 只检查题干和选项是否混入答案还不够；共同阅读材料里如果混入“参考答案：E”等内容，也可能提前显示给答题者。

**怎么解决：** 把阅读材料一起纳入检查。发现疑似混入答案的内容时，先提示核对、修正，不直接作为正常练习内容放行。同时避免自动把可能含答案的完整 PDF 页面放进未答卡片。

**现在的情况：** 已增加相应检查。这是个人练习中的防错措施，仍需要核对，不代表具备正式考试的答案保密能力。

### 5. 开发检查：复杂排版可能让图片跟错题

**发现的问题：** 一个文字块里包含多道题，或者图片跨题、跨栏时，程序可能难以判断图片到底属于哪一道题。

**怎么解决：** 对归属不明确的图片保留原文供核对，并让受影响的题目都进入待核对状态，避免只提示第一题。PDF 可以按题干、具体选项重新框选。

**现在的情况：** 增加了检查和人工修正入口；复杂排版仍不能保证完全自动处理。

### 6. 新需求：从只读 Word 扩展为也能读 PDF

**后来增加的要求：** 既要读文字 PDF，也要读扫描 PDF。

**实现方法：** 文字 PDF 直接读取文字，再按页面位置整理；扫描页使用本地中英文图片文字识别。所需程序和识别资料一起装进 HTML 文件，成品使用时不用联网下载。

**现在的情况：** 文字版、扫描版、两者混合的 PDF 都有处理流程。只要一份 PDF 使用过扫描识别，整份题目都先核对，避免答案区识别错误影响其他题。

### 7. 新需求：单选题必须支持五个选项

**后来增加的要求：** A、B、C、D、E 都要保留，答案为 E 也要正常使用。

**实现方法：** 按原题实际选项生成卡片，重点检查第五项、第五项跨行、大小写和全角标签，以及答案 E 的对应关系。原来的解析规则本身可以处理更多标签，此次补齐并验证了五选项需求。

**现在的情况：** A—E 五选项已经纳入支持和自动检查范围。原文只有四项时不会擅自补一个 E；原文缺项或答案指向不存在的选项时，会提示核对。

### 8. 页面调整：说明太多，希望页面更简洁

按你的要求，已经做了这些修改：

| 原来的内容 | 现在的处理 |
| --- | --- |
| `WORD / PDF → CARDS` | 改成“欢迎使用”。 |
| “中文 · English · 中英混排” | 改成“支持中文、英文、中英混合”。 |
| “准备好这样一份试卷”及下面01、02、03三条说明 | 改成“请保证题目和答案在同一份文档中。” |
| “本地处理 · 文件留在你的设备” | 从页面删除。 |
| “先用示例体验” | 从页面删除。 |
| “原文识别，不生成或猜测答案。” | 从页面删除。 |
| “给复习一个轻松的开始” | 从页面删除。 |
| “无需登录，无广告。关闭或刷新页面后，本次资料与作答会清除。” | 从页面删除。 |
| 默认示例卡片 | 已移除。没有记录时等待导入；有保存记录时恢复自己的试卷。 |
| “你的复习工作台”和“一份试卷，逐题掌握。” | 从首页删除，导入区直接显示文件选择框。 |
| “导入Word或PDF，按原文顺序练习。选好选项，即刻核对答案。” | 从首页删除。 |
| “中英混排 · 文本PDF与扫描PDF” | 整条提示及图标已删除，包括后来短暂保留的“中英混排”。 |

这些文字调整主要是让页面简洁。文件仍在本地处理，程序仍不会猜测原文缺失的答案。旧版刷新后清除资料的行为，已由下面的自动记忆功能替代。

### 9. 交付调整：需要能直接用 VS Code 打开的代码

**后来增加的要求：** 除了可以直接使用的 HTML，也要保留可继续修改的完整代码。

**实现方法：** 提供源码文件夹、VS Code 项目压缩包、工作区配置和中文使用说明。源码就是制作这个程序的原稿；修改源码后，可以重新生成 HTML。

**现在的情况：** 直接使用和修改程序都有对应文件。首次安装开发所需工具和依赖需要联网，日常使用已经生成的 HTML 不需要联网。

### 10. 新需求：关闭后记住试卷，并能手动删除

**原来的问题：** 资料只在浏览器临时内存里，刷新或关闭后，题目修改和作答进度都会消失。

**怎么解决：** 增加浏览器自带的本地数据库，按每份试卷保存题目、图片、原文件副本和最新作答状态。重开恢复最近试卷，列表可以切换其他记录；删除前确认，删除后不再恢复该记录。

**这次还处理了哪些容易出错的地方：** 保存和删除按顺序执行，避免“刚删除又被旧保存任务写回来”；同名试卷使用不同编号，避免互相覆盖；恢复时重新生成原文下载入口，避免旧临时链接失效。保存失败时保留页面内容并提示重试，多页面冲突时不覆盖新数据。

### 11. 实际问题：题目上面多出一行“0”

**你看到的情况：** 导入Word后，“辛亥革命发生在哪一年（）”等题目上面单独显示一个0，看起来像题目被拆成两行。

**原因：** 题号和题目本身读出来是正常的。没有阅读材料时，程序把“材料图片有0张”这个内部结果也显示在了页面上。

**怎么解决：** 改成只有确实存在阅读材料时才显示材料区域。这个改动不删除题目或选项中本来就有的数字0，也不改动答案。

**怎么检查：** 用你提供的Word重现故障，再检查修复后的20道题卡片，确认都不再多出这个0。另外检查了有文字材料、有材料图片，以及题目本身包含0的情况。这是程序自动检查，尚未代替完整的浏览器实际操作验收。

**你需要做什么：** 等页面显示“已自动保存”，再从原来的入口刷新或重新打开新版。已经保存的试卷也会按新方式显示，不需要删除记录或重新导入。

## 五、现在有哪些限制？

- **主要用于单选题。** 多选题不会直接进入“点一次就显示答案”的练习流程，填空、简答等也没有自动作答或评分功能。
- **识别不是百分之百准确。** 扫描模糊、手写、倾斜、特殊字体、复杂分栏和不清楚的题号，都可能需要手工修正。
- **扫描识别目前针对 PDF。** 如果 Word 里面只是贴了整页扫描图片，当前还没有为这种 Word 扩展图片文字识别。
- **一次只处理一份文件，最大20 MB。** PDF 最多100页，其中需要扫描识别的页面最多20页。过大的文件可以拆开导入。
- **处理过久会停止。** 连续120秒没有进度，或整份文件处理超过12分钟，会提示拆分文件。实际快慢取决于电脑和文件内容。
- **加密文件需要先解锁。** 请使用自己能够正常打开的未加密副本；不能靠改文件后缀把其他文件变成 Word 或 PDF。
- **每卷只保存最新进度。** 还没有历次练习成绩、错题本、云同步或账号系统，也没有自动组卷和多人在线考试功能。重新练习会保存清空后的选择，不保留上一轮成绩。
- **旧浏览器可能缺少所需功能。** 如果提示不支持本地识别，可更新 Edge 或 Chrome 后重新打开。

当前程序没有试卷上传接口，导入内容在浏览器内处理并按保存状态写入本地数据库；删除记录不会删除电脑上的原始文档。

## 六、这些文件分别用来干什么？

文件位置：下载并解压后的项目文件夹。

| 文件或文件夹 | 什么时候用 |
| --- | --- |
| [启动拾卷.cmd](启动拾卷.cmd) | **推荐的打开方式。** 用固定的本地地址打开，方便持续读取之前的记录。 |
| [拾卷.html](拾卷.html) | 单文件成品，约16.5 MB。直接打开时，自动保存支持情况取决于浏览器，请查看保存提示。 |
| [拾卷_VSCode项目.zip](拾卷_VSCode项目.zip) | 想用 VS Code 修改程序时，解压后按里面的说明打开项目。 |
| [拾卷_HTML与完整源码.zip](拾卷_HTML与完整源码.zip) | 同时保存成品和完整代码，方便备份或交接。 |
| 解压后的源码文件夹 | 当前完整源码所在位置。修改入口和重新生成方法见其中的 [VSCode使用说明.md](VSCode使用说明.md)。 |
| [拾卷_使用说明.md](拾卷_使用说明.md) | 快速查看怎么打开、导入和练习。 |
| [电子试卷平台项目_完善版.md](电子试卷平台项目_完善版.md) | 详细的需求、计划、技术边界和验收要求，适合后续开发时查阅。 |
| `拾卷_项目说明_通俗版.md` | 就是当前这份说明，用来了解项目现状、修改过程和使用限制。 |

本轮题干上方多余0的修复已同步到HTML和两个源码压缩包，保留自动记忆与手动删除功能；这份通俗说明保存在项目目录中。

## 七、目前检查到什么程度，下一步做什么？

已有开发记录显示，程序通过了多组自动检查，包括：15组 Word 样例共49题，Word 数学结构检查，文字和扫描 PDF 检查，五选项检查，以及断网条件下的导入检查。最新移除示例卡片后，也完成了重新生成 HTML 和11项离线、页面内容及导入检查。

本轮另外检查了保存后恢复、公式图片和原文件保留、快速连续作答、同名试卷、删除后不再出现，以及保存失败、读取失败和删除失败时的处理。这些是模拟数据库和页面状态的开发检查。

这些检查说明已覆盖的情况能按预期工作，**不等于所有真实试卷都能准确识别**。目前还没有完成一套完整的浏览器实际点击、导入和拖动框选验收，也没有用独立真实试卷统计识别准确率。

接下来最有价值的工作是：

1. 用你平时真正做的中文、英文、混合语言试卷试用，覆盖 Word、文字 PDF 和扫描 PDF。
2. 逐题检查题干、选项、公式和参考答案，记录哪份文件、哪道题出了问题。
3. 修正这些真实问题，并保留相应样例，以后修改程序时再检查一次，防止同样的问题重新出现。

按照单人、每周10—15小时的投入，下一阶段优先进行真实浏览器保存与恢复验收，并提高导入和核对的可靠性。自动记忆和手动删除已加入；记录导出备份、错题本和历次成绩仍未完成。
