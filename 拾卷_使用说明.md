# 拾卷使用说明 · 0.4.1

本次修复了题目上方多出一行“0”的显示问题。等“已自动保存”后，在原来使用的入口刷新或重新打开新版即可；不需要删除已保存试卷，也不需要重新导入。题目和选项中本来的数字0仍会保留。

## 打开程序

推荐双击“启动拾卷.cmd”，固定使用它打开的 `http://127.0.0.1:17839/`，并固定使用同一个浏览器的普通窗口。启动器只在本机提供网页，不上传试卷。关闭启动窗口可停止服务，已经保存的记录仍保留。

启动器需要 Node.js 22.13 或更高版本，会尝试使用电脑已安装的 Node.js 或 Codex 自带运行环境；在本项目电脑上可使用现有环境。首次在其他电脑使用且找不到运行环境时，需要先安装 Node.js。使用源码时先生成 `dist/index.html`，再运行启动器或 `pnpm start:local`。

“拾卷.html”仍是完整的单文件网页，可以直接打开。直接打开 HTML 时，不同浏览器对本地文件自动保存的支持可能不同；请以页面保存状态为准，推荐使用上面的固定入口。不同浏览器、隐私窗口或不同打开地址的记录不会自动合并。

## 导入与练习

1. 导入 `.docx`、`.doc` 或 `.pdf`；题目和参考答案需在同一份文档中。
2. 检查识别结果。扫描 PDF、公式原图和不明确的内容先逐题核对；可以修改题干、选项、答案和公式。
3. 单选题支持 A—E 五个选项，按原文顺序显示。选择后立即显示本题参考答案。

每份文件最多20 MB；PDF最多100页，其中需要扫描识别的页面最多20页。低清晰度或复杂排版仍需人工核对。Word 原生公式重新排版；PDF 数学内容主要保留题干和选项原图，不代表所有公式都能转换为可编辑公式。

## 自动记忆

- 导入试卷后自动保存完整题目、参考答案、公式图片、PDF原页和原文件副本。
- 选择答案、保存核对修改、切换练习/核对页签及重新练习后，自动保存最新状态。
- 请等左侧显示“已自动保存”再关闭。刷新或重开后，恢复最近打开的试卷；可以从“已保存试卷”切换其他试卷。
- 同名文件每次导入会建立独立记录。每份试卷保留最新进度，不额外保存每轮历史成绩；编辑窗口中尚未点击保存的草稿不在自动保存范围内。
- “重新练习”清空该卷的选择并保存清空后的状态；题目和核对修改继续保留。
- 保存失败会提示并提供重试，当前页面的内容仍可保留。空间不足时先删除不需要的记录。多页面同时修改同一卷发生冲突时，不覆盖另一页的数据；请先保留需要的修改，再刷新读取最新记录。

## 手动删除

在“已保存试卷”点击对应记录的“删除”，或点击当前试卷底部的“删除本卷记录”，确认后删除这份记录中的题目、修改、作答进度和原文件副本。删除当前记录后回到空页面；其他已保存试卷仍保留。不会删除电脑上原来的 Word 或 PDF 文件。

## 数据存在哪里

记录保存在当前浏览器的本地 IndexedDB 数据库中，不写进 HTML 或源码压缩包，也没有 MySQL 或云端数据库。清理浏览器网站数据、使用隐私窗口或更换电脑可能导致记录不可用；当前未提供跨设备同步和记录导出备份，请保留原试卷。升级前已经丢失的临时内容无法恢复。

自动保存和删除通过了 IndexedDB 模拟与 React 状态流程检查；浏览器实机操作和真实试卷准确率仍需单独验收。
